<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:b="clr-namespace:Prism.Behaviors;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Method635.App.Forms.Views.Team.TeamPage"
             Title="{Binding Title}" >

    <StackLayout>
        <ListView ItemsSource="{Binding TeamList}" SelectedItem="{Binding SelectedTeam}" IsVisible="{Binding HasTeam}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.ContextActions>
                            <MenuItem Command="{Binding LeaveTeamCommand}" Text="Leave Team" CommandParameter="{Binding .}" />
                        </ViewCell.ContextActions>
                        <Grid Padding="5,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <!--<Image Source="{Binding IconSource}" />-->
                            <Label Grid.Column="1" Text="{Binding Name}" />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.Behaviors>
                <b:EventToCommandBehavior EventName="ItemTapped"
                                      Command="{Binding SelectTeamCommand}"/>
            </ListView.Behaviors>
        </ListView>
        <Label IsEnabled="False" IsVisible="{Binding HasNoTeam}" HorizontalTextAlignment="Center"
               VerticalTextAlignment="Center" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <Label.Text>
You don't belong to a team yet.. 
Join or add a team below
            </Label.Text>
        </Label>
        <Button Command="{Binding JoinTeamCommand}" Text="Join Brainstorming Team"/>

        <Button Command="{Binding CreateTeamCommand}" Text="Add Brainstorming Team"/>
    </StackLayout>

</ContentPage>
  
