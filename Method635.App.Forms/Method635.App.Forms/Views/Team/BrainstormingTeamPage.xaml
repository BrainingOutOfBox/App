<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:b="clr-namespace:Prism.Behaviors;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="Method635.App.Forms.Views.Team.BrainstormingTeamPage"
             Title="{Binding Title}" >

    <StackLayout>
        <ListView ItemsSource="{Binding TeamList}" SelectedItem="{Binding SelectedTeam}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Padding="5,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <!--<Image Source="{Binding IconSource}" />-->
                            <Label Grid.Column="1" Text="{Binding Name}" />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.Behaviors>
                <b:EventToCommandBehavior EventName="ItemTapped"
                                      Command="{Binding SelectTeamCommand}"/>
            </ListView.Behaviors>
        </ListView>
        <Button Command="{Binding JoinTeamCommand}" Text="Join Brainstorming Team"/>

        <Button Command="{Binding CreateTeamCommand}" Text="Add Brainstorming Team"/>
    </StackLayout>

</ContentPage>
  
