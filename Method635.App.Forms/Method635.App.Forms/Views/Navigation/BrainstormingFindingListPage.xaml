<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             xmlns:b="clr-namespace:Prism.Behaviors;assembly=Prism.Forms"
             x:Class="Method635.App.Forms.Views.Navigation.BrainstormingFindingListPage"
             Title="{Binding Title}">
    <StackLayout>
        <ListView ItemsSource="{Binding FindingList}" SelectedItem="{Binding SelectedFinding}" IsVisible="{Binding HasFindings}" IsPullToRefreshEnabled="True" RefreshCommand="{Binding RefreshFindingListCommand}" IsRefreshing="{Binding IsRefreshing}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Padding="5,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <!--<Image Source="{Binding IconSource}" />-->
                            <Label Grid.Column="1" Text="{Binding Title}" />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.Behaviors>
                <b:EventToCommandBehavior EventName="ItemTapped"
                                      Command="{Binding SelectFindingCommand}"/>
            </ListView.Behaviors>
        </ListView>
        <Label IsEnabled="False" IsVisible="{Binding HasNoFindings}" HorizontalTextAlignment="Center"
               VerticalTextAlignment="Center" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <Label.Text>
There are no brainstorming findings for this team yet.. 
Add a brainstorming finding below or wait till another team member creates one.
            </Label.Text>
        </Label>
        <Button Command="{Binding CreateFindingCommand}" Text="Add Brainstorming Finding"/>
    </StackLayout>
</ContentPage>